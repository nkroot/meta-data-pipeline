pip install -r requirements.txt  - to install all the packages from the requirements to the environment

git setup
---------

git init
git add --all

git config --global user.email "naveenkollati431@gmail.com"
git config --global user.name naveen

git commit -m "message"

git branch -M main
git remote add origin https://github.com/nkroot/meta-data-pipeline.git
git push -u origin main -- push it to git 

docker
--------

docker build -t metadata -- to build an image
docker-compose up -d 
docker-compose down -- to down everything

docker-compose build --no-cache -- used to rebuild the existing build without using cache build.
docker ps -- to see the containers
docker ps -a to see all the containers
docker image ls -- list all images

docker exec -it 97b69290ae2d /bin/bash -- to see the contents of container eg postgres querying environmnt